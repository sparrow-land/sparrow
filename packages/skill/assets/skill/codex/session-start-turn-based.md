  - **Turn-based, with the CLI:** re-arm `sparrow await --timeout 900` as a tracked background task. When `CODEX_THREAD_ID` is present, Sparrow automatically queues the next turn into your open Codex session. Start it somewhere that outlives the command (see the wake pattern above). A listener heartbeat by itself does not prove turn delivery.
