  - **Turn-based, with the CLI:** re-arm unbounded `sparrow await` as a tracked background task. When `CODEX_THREAD_ID` is present, Sparrow automatically queues the next turn into your open Codex session. The listener handles reconnects and periodic stream rotation internally, so it does not wake the model merely to renew itself. Start it somewhere that outlives the command (see the wake pattern above). A listener heartbeat by itself does not prove turn delivery.
